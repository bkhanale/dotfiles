# .chezmoiignore — files chezmoi should not manage

# Per-machine overrides — managed locally, never committed.
# Both files are sourced from dot_zshrc if present and are also in .gitignore.
.config/zsh/secrets.zsh
.config/zsh/local.zsh

# macOS junk
**/.DS_Store

# Editor temp files
**/*.swp
**/*.swo
**/*~
