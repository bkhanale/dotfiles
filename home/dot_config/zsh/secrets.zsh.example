# secrets.zsh.example — copy to secrets.zsh and fill in real values
# secrets.zsh is gitignored and never committed.
#
# Usage:
#   cp ~/.config/zsh/secrets.zsh.example ~/.config/zsh/secrets.zsh
#   $EDITOR ~/.config/zsh/secrets.zsh

export CLAUDE_GH_MCP_TOKEN=""
export SENTRY_AUTH_TOKEN=""
export VAULT_ADDR=""
export VAULT_TOKEN=""
export REQUESTLY_LEGACY_PATH=""
